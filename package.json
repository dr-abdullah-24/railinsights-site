{
  "name": "railinsights-td-server",
  "version": "1.0.0",
  "description": "WebSocket bridge — Confluent Kafka TD stream → browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
